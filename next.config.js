/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'res.cloudinary.com', 'cdn.iconscout.com', 'assets.coingecko.com'],
    }

}

module.exports = nextConfig
